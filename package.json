{
  "name": "mameru.dev-api",
  "module": "index.ts",
  "type": "module",
  "scripts": {
      "build": "bun build index.ts --outdir public --outfile index.js",
      "start": "bun run index.ts",
      "dev": "bun run index.ts"
  },
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5.0.0"
  },
  "dependencies": {
    "chalk": "^5.3.0"
  }
}
